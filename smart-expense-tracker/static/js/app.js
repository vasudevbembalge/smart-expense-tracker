document.addEventListener('DOMContentLoaded',()=>loadTransactions());document.getElementById('transaction-form').addEventListener('submit',function(e){e.preventDefault();const type=document.getElementById('type').value;const description=document.getElementById('description').value;const amount=document.getElementById('amount').value;fetch('/add_transaction',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type,description,amount})}).then(r=>r.json()).then(d=>{if(d.success){document.getElementById('transaction-form').reset();loadTransactions();}})});function loadTransactions(){fetch('/get_transactions').then(r=>r.json()).then(d=>{updateBalance(d.balance);displayTransactions(d.transactions);});}function updateBalance(b){document.getElementById('total-income').textContent=`₹${b.total_income.toFixed(2)}`;document.getElementById('total-expense').textContent=`₹${b.total_expense.toFixed(2)}`;document.getElementById('remaining').textContent=`₹${b.remaining.toFixed(2)}`;}function displayTransactions(transactions){const listDiv=document.getElementById('transactions-list');if(transactions.length===0){listDiv.innerHTML='<p class="no-data">No transactions yet. Add one above!</p>';return;}listDiv.innerHTML='';transactions.slice().reverse().forEach(trans=>{const transDiv=document.createElement('div');transDiv.className=`transaction-item ${trans.type}`;transDiv.innerHTML=`<div class="transaction-details"><h4>${trans.description}</h4><p>${trans.date}</p></div><div style="display:flex;align-items:center;"><span class="transaction-amount ${trans.type}">${trans.type==='income'?'+':'-'}₹${trans.amount.toFixed(2)}</span><button class="btn-delete" onclick="deleteTransaction(${trans.id})">Delete</button></div>`;listDiv.appendChild(transDiv);});}function deleteTransaction(id){if(confirm('Are you sure you want to delete this transaction?')){fetch(`/delete_transaction/${id}`,{method:'DELETE'}).then(r=>r.json()).then(d=>{if(d.success)loadTransactions();});}}